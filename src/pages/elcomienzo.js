export const prerender = false;

export function GET({ redirect }) {
  return redirect('https://distrokid.com/hyperfollow/mateoferro/el-comienzo?utm_campaign=website&utm_medium=Email+&utm_source=SendGrid', 302);
}
